# 2026-09-06

- 完成引导（BOOTSTRAP）：确立身份「小课」，称呼用户为「同学」，已删除 BOOTSTRAP.md。
- 确认需求后从零创建「小课表」微信小程序 v1（原生框架，仅本地存储）：
  - 用户追加要求：课程可一键添加到多个周（周次自由组合）、界面要有日期显示 → 已实现周次 chips 多选 + 一键预设（1-16周/全选/单周/双周/清空），周课表列头与今日页均显示日期。
  - 结构：三个 tab（今日/课程表/设置）+ 课程编辑页；utils/store.js（存储）+ utils/time.js（周次/节次计算）。
  - 功能：课程增删改查、彩色课程卡、按周切换、今日状态（上课中/倒计时/已结束）、应用内上课提醒横幅、节次时间自定义、数据导出导入（剪贴板 JSON）、示例课表、长按周列空白快速添加。
  - 数据模型：course = {id,name,teacher,location,day(1-7),startSection,endSection,weeks:[周次数组],color,note}。
  - 工具：tools/gen_icons.py（PIL 生成 6 张 81px tab 图标）；preview.html 静态视觉预览（三屏）。
  - 自检：node --check 全部 JS 通过；JSON 全部有效；图标生成成功。
- 待办/可选：微信订阅消息推送需后端或云开发（当前仅应用内提醒）；深色模式、课表分享图片未做。
